import ProfileLayout from "@/components/profile/ProfileLayout";

const ProfileInfo = () => {
  return (
    <ProfileLayout>
      <div>favorite</div>
    </ProfileLayout>
  );
};

export default ProfileInfo;
